$(document).on('turbolinks:load', () => {
  Vue.component('mapv', {
    template: '',
    data () {
      return {
        toto: 'Hello'
      }
    },
    created () {
      console.log('RightSidebar Mapv created')
    },
    // methods: {

    // }
  })
})
